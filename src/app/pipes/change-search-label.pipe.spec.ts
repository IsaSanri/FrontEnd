import { ChangeSearchLabelPipe } from './change-search-label.pipe';

describe('ChangeSearchLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeSearchLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
